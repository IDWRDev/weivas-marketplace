name: Legacy GitHub Pages notice
on: workflow_dispatch
jobs:
  explain:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Weivas now requires a server-capable Next.js host. See docs/AUTHENTICATED-DEPLOYMENT.md."
