import { Download, Filter, MoreHorizontal, Plus, Upload } from "lucide-react";
import { products } from "@/data/mock/marketplace";
import { Card, Badge } from "@/components/ui/Primitives";
export default function Products(){return <><header className="dash-head"><div><small>CATALOGUE</small><h1>Products</h1><p>Manage, verify and organise your complete catalogue.</p></div><div><button className="button secondary"><Upload/> Bulk upload</button><button className="button secondary"><Download/> Export</button><button className="button"><Plus/> Add new product</button></div></header><div className="metrics four">{[["Total products","256"],["Active products","198"],["Under review","24"],["Out of stock","34"]].map(x=><Card key={x[0]}><small>{x[0]}</small><strong>{x[1]}</strong><p>Updated today</p></Card>)}</div><Card className="table-card"><div className="filters"><input aria-label="Search products" placeholder="Search by product name or SKU…"/><select aria-label="Category"><option>All categories</option>{[...new Set(products.map(p=>p.category))].map(c=><option key={c}>{c}</option>)}</select><select aria-label="Status"><option>All statuses</option>{["Draft","Submitted","Under Review","Approved","Rejected","Active","Low Stock","Out of Stock","Suspended"].map(s=><option key={s}>{s}</option>)}</select><button className="button secondary"><Filter/> More filters</button></div><table><thead><tr><th>Product</th><th>SKU</th><th>Category</th><th>Price</th><th>Stock</th><th>Sales</th><th>Verification</th><th>Status</th><th>Actions</th></tr></thead><tbody>{products.map((p,i)=><tr key={p.id}><td data-label="Product"><span>{p.emoji}</span><b>{p.name}</b></td><td data-label="SKU">{p.sku}</td><td data-label="Category">{p.category}</td><td data-label="Price"><b>${p.price}</b></td><td data-label="Stock">{p.stock}</td><td data-label="Sales">{(p.sold||0).toLocaleString()}</td><td data-label="Verification"><Badge tone={p.productVerified?"green":"orange"}>{p.productVerified?"Verified":"Pending"}</Badge></td><td data-label="Status"><Badge tone={p.status==="Out of Stock"?"red":p.status==="Active"?"green":"orange"}>{p.status}</Badge></td><td data-label="Actions"><button aria-label={`Open actions for ${p.name}`} title="View, edit, duplicate, archive, delete or restock"><MoreHorizontal/></button><span className="sr-only">{i===0?"View Edit Duplicate Archive Delete Restock":"Product actions"}</span></td></tr>)}</tbody></table></Card></>}
