import Link from "next/link";import { KeyRound,ShieldCheck } from "lucide-react";import { Card } from "@/components/ui/Primitives";import { requireArea } from "@/server/auth/session";import { db } from "@/server/db/client";import { changePassword } from "../actions";
export default async function Page(){const session=await requireArea("account");const events=await db.securityEvent.findMany({where:{userId:session.user.id},orderBy:{createdAt:"desc"},take:10});return <><header className="dash-head"><div><small>ACCOUNT PROTECTION</small><h1>Security</h1><p>Manage your password and review recent security activity.</p></div><ShieldCheck aria-hidden="true"/></header><div className="dash-grid"><Card className="span2"><h2>Change password</h2><form action={changePassword} className="settings-form"><label>Current password<input type="password" name="currentPassword" autoComplete="current-password" required/></label><label>New password<input type="password" name="password" autoComplete="new-password" minLength={10} required/></label><label>Confirm new password<input type="password" name="passwordConfirmation" autoComplete="new-password" minLength={10} required/></label><button className="button"><KeyRound/>Update password</button></form></Card><Card><h2>Sessions</h2><Link className="button" href="/api/auth/signout">Sign out this session</Link><p>Sign out all sessions and two-factor authentication require the future session-revocation provider.</p></Card><Card className="span2"><h2>Recent security events</h2>{events.length?events.map(event=><p key={event.id}><b>{event.type.replaceAll("_"," ")}</b> · {event.createdAt.toLocaleString()}</p>):<p>No recent security events.</p>}</Card></div></>}
