import Link from "next/link";import { Badge,Card } from "@/components/ui/Primitives";import { requireArea } from "@/server/auth/session";import { db } from "@/server/db/client";import { updateSupportTicket } from "./actions";
export const dynamic="force-dynamic";
export default async function Page(){await requireArea("admin");const tickets=await db.supportTicket.findMany({orderBy:{createdAt:"desc"},take:100});return <main className="admin-page"><header><div><small>WEIVAS OPERATIONS</small><h1>Support queue</h1><p>Customer requests stored by the operational support intake.</p></div><Link className="button secondary" href="/admin">Admin overview</Link></header><Card className="table-card">{tickets.length?tickets.map(ticket=><article className="support-ticket" key={ticket.id}><header><div><b>{ticket.reference}</b><small>{ticket.createdAt.toLocaleString()} · {ticket.category}</small></div><Badge tone={ticket.status==="resolved"||ticket.status==="closed"?"green":"orange"}>{ticket.status.replaceAll("_"," ")}</Badge></header><h2>{ticket.subject}</h2><p>{ticket.message}</p><small>{ticket.name} · {ticket.email}</small><form action={updateSupportTicket}><input type="hidden" name="id" value={ticket.id}/><label>Status<select name="status" defaultValue={ticket.status}><option value="open">Open</option><option value="in_progress">In progress</option><option value="waiting_on_customer">Waiting on customer</option><option value="resolved">Resolved</option><option value="closed">Closed</option></select></label><button className="button">Update</button></form></article>):<p>No support requests.</p>}</Card></main>}
