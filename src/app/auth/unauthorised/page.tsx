import Link from "next/link";import { AuthShell } from "@/components/auth/AuthShell";
export default async function Page({searchParams}:{searchParams:Promise<{reason?:string}>}){const {reason}=await searchParams;return <AuthShell eyebrow="ACCESS PROTECTED" title={reason==="account-status"?"Your account needs attention":"You cannot open this area"} copy="Weivas enforces account status, role and ownership on the server."><h2>Access unavailable</h2><p>{reason==="account-status"?"Suspended or disabled accounts cannot use protected features. Contact support for assistance.":"Sign in with an authorised account or return to the marketplace."}</p><Link className="button" href="/">Return to marketplace</Link></AuthShell>}
