import Link from "next/link";import { AuthShell } from "@/components/auth/AuthShell";import { SignInForm } from "@/components/auth/AuthForms";
export default async function Page({searchParams}:{searchParams:Promise<{callbackUrl?:string;registered?:string;reset?:string}>}){const params=await searchParams;return <AuthShell eyebrow="WELCOME BACK" title="Good things are waiting." copy="Sign in to manage your Weivas account and protected marketplace activity."><h2>Sign in</h2>{(params.registered||params.reset)&&<p role="status" className="form-success">Your account is ready. Sign in to continue.</p>}<SignInForm callbackUrl={params.callbackUrl}/><p>New to Weivas? <Link href="/auth/register">Create an account</Link></p></AuthShell>}
