import { withAuth } from "next-auth/middleware";import { NextResponse } from "next/server";
export default withAuth(function proxy(request){const token=request.nextauth.token;const path=request.nextUrl.pathname;if(!token)return NextResponse.next();if(token.status!=="active")return NextResponse.redirect(new URL("/auth/unauthorised?reason=account-status",request.url));if(path.startsWith("/admin")&&token.role!=="admin")return NextResponse.redirect(new URL("/auth/unauthorised",request.url));if(path.startsWith("/seller")){if(token.role!=="seller")return NextResponse.redirect(new URL(token.sellerApplicationStatus?"/sell/status":"/sell/onboarding",request.url));if(token.sellerApplicationStatus!=="approved")return NextResponse.redirect(new URL("/sell/status",request.url));}return NextResponse.next()},{callbacks:{authorized:({token})=>Boolean(token)}});
export const config={matcher:["/account/:path*","/seller/:path*","/admin/:path*"]};
