import "server-only";import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import { authOptions } from "./config";import { canAccessProtectedArea } from "./permissions";
export async function getSession(){return getServerSession(authOptions)}
export async function requireArea(area:"account"|"seller"|"admin"){const session=await getSession();const access=canAccessProtectedArea(session?.user,area);if(access.allowed)return session!;if(access.reason==="sign_in")redirect(`/auth/sign-in?callbackUrl=/${area}`);if(access.reason==="seller_application")redirect("/sell/onboarding");if(access.reason==="seller_status")redirect("/sell/status");if(access.reason==="account_status")redirect("/auth/unauthorised?reason=account-status");redirect("/auth/unauthorised")}
