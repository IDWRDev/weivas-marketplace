import Link from "next/link"; import { Heart, Star } from "lucide-react"; import type { Product } from "@/types/marketplace";
export function ProductCard({product}:{product:Product}){return <article className="product-card"><button aria-label={`Save ${product.name}`}><Heart/></button><Link href={`/product/${product.id}`}><div className="product-visual" role="img" aria-label={product.name}>{product.emoji}</div><p>{product.name}</p><strong>${product.price.toFixed(2)}</strong>{product.oldPrice&&<del>${product.oldPrice}</del>}<small><Star fill="currentColor"/> {product.rating} · verified</small></Link></article>}
