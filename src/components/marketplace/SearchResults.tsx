"use client";
import { useMemo, useState } from "react";
import { SlidersHorizontal } from "lucide-react";
import { useSearchParams } from "next/navigation";
import { categories, products } from "@/data/mock/marketplace";
import { ProductCard } from "@/components/marketplace/ProductCard";
export function SearchResults(){const params=useSearchParams();const q=params.get("q")??"";const [category,setCategory]=useState("All categories");const [sort,setSort]=useState("recommended");const filtered=useMemo(()=>products.filter(p=>(p.name+" "+p.category+" "+p.brand).toLowerCase().includes(q.toLowerCase())&&(category==="All categories"||p.category.includes(category.split(" & ")[0]))).toSorted((a,b)=>sort==="price-low"?a.price-b.price:sort==="rating"?b.rating-a.rating:(b.sold||0)-(a.sold||0)),[q,category,sort]);return <main className="section search-page"><span>SEARCH RESULTS</span><h1>{q?`Results for “${q}”`:"Explore all products"}</h1><div className="search-toolbar"><button className="button secondary"><SlidersHorizontal/> Filters</button><label>Category<select value={category} onChange={e=>setCategory(e.target.value)}><option>All categories</option>{categories.map(c=><option key={c.name}>{c.name}</option>)}</select></label><label>Sort by<select value={sort} onChange={e=>setSort(e.target.value)}><option value="recommended">Recommended</option><option value="rating">Top rated</option><option value="price-low">Price: low to high</option></select></label><span>{filtered.length} products</span></div>{filtered.length?<div className="product-grid">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty"><b>No products found</b><p>Try a broader search or browse another category.</p></div>}</main>}
